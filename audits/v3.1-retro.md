# v3.1 Retro — frozen at tag `v3.1` (2026-07-03)

Release window: 2026-05-27 (v3.0 tagged to `main`) → 2026-07-03 (v3.1 tagged). All work on `develop`. Per §8, this retro is a frozen point-in-time record; the living registry stays in [`gaps.md`](gaps.md).

## What shipped

**1. Post-v3.0 folds (May–June).**
- All 14 v3.0 deprecation stubs removed; legacy pre-v3 paths now 404 (init/audit skills detect and migrate).
- `recipes/activity-stream.md` shipped Drafted (multi-source capture: Stop hook + post-commit hook + `/log` + `/refine-log`), replacing the `consulting-timesheet.md` placeholder.
- Eight folds from external research (Osmani/Saboo/Kartakis *The New SDLC with Vibe Coding*; Osmani *Agentic Code Review*): §0.6b AI-discipline spectrum, §3 tests-vs-evals, §3d intent-reconstruction reframing + three-axis risk decomposition + test-change verification, six new §0.6 anti-patterns.
- Multi-model readability sweep (4 frontier providers via `b1review`) → ~18 link fixes, tier-terminology disambiguation, Where to Start table, and the second `check-refs.sh` invariant (relative links).
- §0.9 Adoption Ledger (default-deny, convergence read, retirement pass, 30-day litmus) — unanimous design convergence across the 4-model panel; ADR 0005.
- Agentic-lifecycle coverage: §3f eval suites, §14b prompt engineering, §14c model pinning, §14d agent-failure debugging — each shipped pre-gated.

**2. The 2026-07-03 deep-inspection + structural pass** (commit `5401b29`; the bulk of v3.1). Four critical-reader agents over the full corpus, then a maintainer-approved "apply all":
- 20 verified correctness/hazard fixes (stale token claims, §3d Layer-2/Trivial contradiction, §9.3 reality-wins clause, §29.8 mic/camera scoping, retention-table handwaves, activity-stream Stop-hook schema bug, missed §3d→skill mirror, and more).
- Landing slimmed 144KB → 118KB by extracting the §0.6 anti-pattern catalog to `core/umami-anti-patterns.md` (verified byte-lossless); landing size became a budgeted, checked quantity.
- `ext/umami-backend.md` (§31) — closing the "most common adopter project type falls through every extension" gap.
- §16/§24 stripped of embedded-textbook content; all pre-§0.9 extensions retro-gated; audience tags on product-builder sections; §3d gained solo-scale degradation and new-dependency→High.
- Adoption surface: tag-pinned distribution, dual licensing (`LICENSE-CODE` MIT for code artifacts), README Quick-start restructure, `recipes/claude-md-starter.md`, `.github/ISSUE_TEMPLATE/validation-report.md` (the validation funnel).
- Verified by an internal 4-agent adversarial sweep plus a 4-model `b1review` pre-merge pass (all four: MERGE WITH FIXES); confirmed findings fixed, false positives discarded.

**3. Release chores (at the tag).**
- **CMS rollup:** §20 (WordPress) and §21 (Drupal) folded into `ext/cms/umami-cms.md`; the two variant files deleted. §20.x/§21.x heading numbers preserved inside the CMS file — sections moved files, nothing renumbered.
- **First firing of the §0.9 reverse gate:** `recipes/closed-loop-pr-review.md`, a Planned placeholder since v3.0 with no field implementation, was deprecated per the gap-registry disposition ("default per §0.9 when ambiguous: deprecate"). §30.5 + §3d carry the guidance. The mechanism worked as designed — a forced disposition at the release boundary actually removed process instead of accreting it.
- **Third `check-refs.sh` invariant:** the landing fails the fitness check past 125,000 bytes (≈ ~25K tokens).

## What slipped / was deferred

- Tier-D single-source items from the June sweep (≤5-rec cap ambiguity, §6b adopt-vs-depth, §15 checklist tier-tagging, uneven cost-profile annotation) — still deferred, tracked in `gaps.md`.
- Deeper `core/umami-quality.md` compression (merging watch-signal/failure-mode table pairs across §2b/§3/§3d) — the enumerated redundancy cuts landed; the systematic merge did not.
- An adopter-facing changelog / tag-diff artifact — deliberately deferred until the first team actually pins a tag (per the structural-pass gap entry).

## Decisions of record

- Landing token budget: ~25K (125KB), enforced by fitness function; growth extracts, never absorbs (gap entry closes after one clean release cycle).
- Distribution now offers immutable-tag pinning as the recommended team posture; `main` remains the default for solo/experimental use.
- §20/§21 section numbers remain reserved and live inside `ext/cms/umami-cms.md`; no new extension may claim them. Next available extension section after this release: **§32**.
- The corpus's evidence base remains self-validated (multi-model review + maintainer projects, zero external adopters). v3.1's bet is that the starter recipe + validation-report template convert readers into reporters; the ~28 open "not yet validated" entries all resolve through that funnel or not at all.
